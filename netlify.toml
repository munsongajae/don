[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  
# Node.js 22가 필요하지만 Netlify에서 22를 지원하지 않을 수 있으므로 20으로 시도
# 필요시 Netlify 대시보드에서 직접 설정

